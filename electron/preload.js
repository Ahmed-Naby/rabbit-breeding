// Preload script for safe IPC communications
// Extend window.electron API if needed here

window.addEventListener("DOMContentLoaded", () => {
  // Can execute DOM-based checks/initialization if needed
});
